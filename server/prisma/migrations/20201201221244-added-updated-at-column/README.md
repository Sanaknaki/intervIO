# Migration `20201201221244-added-updated-at-column`

This migration has been generated by Ali Sanaknaki at 12/1/2020, 5:12:44 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;
CREATE TABLE "new_Application" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "position" TEXT NOT NULL,
    "company" TEXT NOT NULL,
    "website" TEXT NOT NULL,
    "status" TEXT NOT NULL,
    "postedById" INTEGER,
    "interviewRounds" INTEGER NOT NULL DEFAULT 0,

    FOREIGN KEY ("postedById") REFERENCES "User"("id") ON DELETE SET NULL ON UPDATE CASCADE
);
INSERT INTO "new_Application" ("id", "createdAt", "position", "company", "website", "status", "postedById", "interviewRounds") SELECT "id", "createdAt", "position", "company", "website", "status", "postedById", "interviewRounds" FROM "Application";
DROP TABLE "Application";
ALTER TABLE "new_Application" RENAME TO "Application";
PRAGMA foreign_key_check;
PRAGMA foreign_keys=ON
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201130190233-init..20201201221244-added-updated-at-column
--- datamodel.dml
+++ datamodel.dml
@@ -3,14 +3,15 @@
 }
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 model Application {
   id              Int      @id @default(autoincrement())
   createdAt       DateTime @default(now())
+  updatedAt       DateTime @default(now())
   position        String
   company         String
   website         String
   status          String
```


